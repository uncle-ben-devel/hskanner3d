# this file contains the default settings for the scanner. you can copy it and rename it to hskanner.config for it to be used by the processing chain.
# paths
	meshroomroot="$HOME/Documents/software/Meshroom-2019.2.0-linux/Meshroom-2019.2.0" # install directory of meshroom"
	workpath=/media/hskanner3d-dev/clonezill_image_/devel_folder/processing_chain	# root of processing chain
	#pipe_file="compute_pointcloud.mg"
	pipe_file="compute_tex_model.mg"
	#timestamp=$(date "+%d-%h-%Y-%T")	# name for the folder where the images will be stored
	timestamp="put-date-or-indexer-in-here-with-gui"
# settings
	# taking photos
		number_sensor_nodes="1"	# number of cameras connected to the CN
		network_subnet="192.168.128.0/24"
		shutterspeed="16.67"	# shutter speed in ms. 16.67ms equals about 1/60s.
		isosetting="100"	# ISO. Max is 1600, Min is 50.
		whitebalance="auto"	# auto cloudy flash fluorescent horizon incandescent shade sunlight tungsten or red / blue gain in floating point (typical between 1.0 and 2.0)
		selftimer="0"	# waiting time before capture in seconds. Base wait time is always 0.5s to make a sync capture possible.
		rotation="180"	#Rotate the output images by 0, 90, 180 or 270 degrees.
		openfilebrowser="1"	# bool if the filebrowser should be opened after the shots are taken
	# processing photos
		
	# generating 3d data
		view_mesh="1"	# if 0, will not open meshlab to view the generated mesh
	# processing 3d data
